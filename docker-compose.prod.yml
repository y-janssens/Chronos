version: '3.7'

services:

  sample-prod:
    container_name: slash-web-1
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - '1337:80'
networks:
  frontend:
    driver: nginxproxymanager_default
